# =============================================================
#  NEXUS — AI Radiomics Platform  |  requirements.txt
#  Install with:  pip install -r requirements.txt
# =============================================================

# Web framework
flask>=2.3.0
flask-cors>=4.0.0

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0

# Scientific computing
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# 3-D mesh support (used by HMB helpers)
trimesh>=4.0.0

# Hyperparameter optimisation (used by HMB helpers)
optuna>=3.3.0

# Warning suppression
shutup>=0.2.0

# =============================================================
#  OPTIONAL — Deep Learning (Tab 3 & CNN Prediction)
#  Install TensorFlow separately based on your hardware:
#
#  CPU-only:
#    pip install tensorflow-cpu
#
#  GPU (NVIDIA CUDA 11/12):
#    pip install tensorflow
#
#  The CNN tabs are automatically disabled if TensorFlow
#  is not detected; all other tabs remain fully functional.
# =============================================================
